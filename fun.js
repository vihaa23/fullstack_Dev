const hello = (fname,lname) =>{
    console.log(`hello ${fname} ${lname}`);//$-used to convert variableto string
}

module.exports={greeting:hello}//module.export helps to export a particular file in another file